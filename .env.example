# Google Cloud Web OAuth Client ID（選填的部署預設值）。
# 未設定時，教師可在 App 的「系統設定」自行填入；不要放 Client secret 或 access token。
# Google Cloud 必須啟用 Forms API，並把本機與正式網址加入「已授權的 JavaScript 來源」。
VITE_GOOGLE_OAUTH_CLIENT_ID=
