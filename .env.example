# Google Cloud Web OAuth Client ID（部署管理者填入）。
# 建置時注入後，教師可一鍵建立 Google Form，無需自行設定。
# 靜態部署包也可改填 public/deploy-config.json；不要放 Client secret 或 access token。
# Google Cloud 必須啟用 Forms API，並把本機與正式網址加入「已授權的 JavaScript 來源」。
VITE_GOOGLE_OAUTH_CLIENT_ID=
